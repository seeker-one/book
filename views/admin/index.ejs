
<!DOCTYPE html>
<html lang="zh-CN">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- 上述3个meta标签*必须*放在最前面，任何其他内容都*必须*跟随其后！ -->
    <meta name="description" content="">
    <meta name="author" content="">
    <link rel="icon" href="../../favicon.ico">

    <title>后台管理页面</title>

    <!-- Bootstrap core CSS -->
    <link href="/stylesheets/bootstrap.min.css" rel="stylesheet">

    <link href="/stylesheets/dashboard.css" rel="stylesheet">
     <style type="text/css">
       body{
        overflow-y:scroll; 
       }
    </style>
  </head>

  <body>

    <% include header.ejs%>
   
    <div class="container-fluid">
      <div class="row">
         <% include nav.ejs%>
        <div class="col-sm-9 col-sm-offset-3 col-md-10 col-md-offset-2 main">
          <h1 class="page-header">添加书籍类别</h1>
          <div class="row">
              <form class="form-horizontal" role="form" name="addTypeForm" >
                  <div class="form-group">
                      <label class="col-md-2 control-label">
                          新的类别：
                      </label>
                      <div class="col-md-5">
                          <input type="text" class="form-control" placeholder="请输入类别名……"  id="typeName" name="typeName" required autofocus="">
                      </div>
                  </div>
                  <div class="form-group">
                      <div class="col-md-offset-2 col-md-5">
                          <input class="btn btn-primary" type="button" value="添加" id='submitBtn'>
                          <input class="btn btn-default" type="reset">
                      </div>
                  </div>
              </form>
          </div>

    
        </div>
      </div>
    </div>

    <script src="/javascripts/jquery.min.js"></script>
    <script src="/javascripts/bootstrap.js"></script>
    <script>
        $(function(){
            $('#submitBtn').click(function(){
                $.ajax({
                   url:'/admin/addType',
                   type:'post',
                   data:{
                      sort_name:$('#typeName').val()
                   },
                   success:function(data){                
                       alert(data.msg);
                       $('#typeName').val('');
                   },
                   error:function(){
                      
                   }
                })
            })
        })

        $(function(){
            $("ul#nav li").eq(0).attr('class','active');
        })
    </script>
  </body>
</html>
